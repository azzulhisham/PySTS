# MANTIS MCP — small Python MCP server for the MANTIS REST API
#
# Install:
#   cd PySTS/mcp
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# Copy env:
#   cp .env.example .env
#
# Run (stdio — used by Cursor):
#   python server.py

# Pin to 1.x — FastMCP lives in mcp.server.fastmcp (removed/changed in mcp 2.0)
mcp>=1.9.0,<2
httpx>=0.27.0
python-dotenv>=1.0.0
